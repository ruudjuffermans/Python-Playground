"""A passing test."""


def test_passing():
    """
    Test to verify that two identical tuples are equal.

    This function asserts that the tuple (1, 2, 3) is equal to itself.
    It's an example of a passing test, as the assertion condition will always be true.
   
    Returns:
        None: This function is designed for testing and does not return a value.
    """
    assert (1, 2, 3) == (1, 2, 3)
